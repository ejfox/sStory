section_type schema

sections: {
  photo: {
    photoBigText: {
      inputs: ['url', 'title']
    },
    photoCaption: {
      inputs: ['url', 'caption']
    }
  },
  video: {
    videoYoutube: {
      inputs: ['embedCode', 'caption']
    }
    videoVimeo: {
      inputs: ['embedCode', 'caption']
    }
  }
  
  
}  